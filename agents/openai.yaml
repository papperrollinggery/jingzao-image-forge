interface:
  display_name: "镜造 Image Forge"
  short_description: "Compile reference-aware image specs and clean prompts"
  default_prompt: "Use $jingzao-image-forge to ground this brief in a real delivery purpose, attach every necessary reference through a verified call plan and receipt, apply the adaptive clean base without flattening intentional texture, budget review from semantic complexity without truncation, remove contamination, map UI Motion into explicit L0-L3 roles when relevant, and return a validated spec plus a platform-ready prompt with a visual acceptance gate."
policy:
  allow_implicit_invocation: true
