interface:
  display_name: "镜造 Image Forge"
  short_description: "Distill references into faithful, testable image prompts"
  default_prompt: "Use $jingzao-image-forge to ground this brief in a real delivery purpose, preserve target-adopted reference hair, expression, pose/action, body and wardrobe through a reference profile when relevant, attach every necessary reference through a verified call plan and receipt, apply the adaptive clean base without flattening intentional texture, budget review from semantic complexity without truncation, remove contamination, map UI Motion into explicit L0-L3 roles when relevant, and return a validated spec plus a platform-ready prompt with a visual acceptance gate."
policy:
  allow_implicit_invocation: true
