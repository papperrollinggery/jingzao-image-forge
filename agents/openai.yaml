interface:
  display_name: "镜造 Image Forge"
  short_description: "Direct verified references, cinematic shots, styles, and prompts"
  default_prompt: "Use $jingzao-image-forge to ground this brief in a real delivery purpose, attach every necessary reference through a verified call plan and receipt, protect the requested style and invariants, remove semantic contamination or repetition, and return a validated spec plus a platform-ready prompt."
policy:
  allow_implicit_invocation: true
