interface:
  display_name: "镜造 Image Forge"
  short_description: "Direct cinematic specs, spectacle, styleboards, and image prompts"
  default_prompt: "Use $jingzao-image-forge to turn this image brief into a validated visual spec and platform-ready prompt."
policy:
  allow_implicit_invocation: true
